/**
 * Simple export of the toy scenario for use in examples and tests.
 */
import { createToyScenario } from '../src/toy/toyScenario.js';

const scenario = createToyScenario({});

export default scenario;
